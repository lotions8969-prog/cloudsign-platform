apiVersion: v1
kind: Namespace
metadata:
  name: all-contract-local
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: all-contract-frontend
  namespace: all-contract-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: all-contract-frontend
  template:
    metadata:
      labels:
        app: all-contract-frontend
    spec:
      containers:
        - name: frontend
          image: all-contract-frontend
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: development
            - name: NEXT_PUBLIC_FIREBASE_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: all-contract-secrets
                  key: firebase-project-id
                  optional: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: all-contract-frontend
  namespace: all-contract-local
spec:
  selector:
    app: all-contract-frontend
  ports:
    - port: 3000
      targetPort: 3000
  type: LoadBalancer
